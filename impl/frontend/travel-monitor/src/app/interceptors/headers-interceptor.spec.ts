import { HeadersInterceptor } from './headers-interceptor';

describe('HeadersInterceptor', () => {
  it('should create an instance', () => {
    expect(new HeadersInterceptor()).toBeTruthy();
  });
});
